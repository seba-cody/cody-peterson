# cody-peterson.com

Cody Peterson's personal scholar site (GitHub `seba-cody/cody-peterson`,
Cloudflare Worker `cody-peterson`, custom domains via dashboard — never
proxied CNAMEs to workers.dev). Moved here from `~/projects/depth/cody-peterson`
on 2026-07-04.

- Purpose: carries the Person JSON-LD @graph in a bilateral schema handshake
  with seba.health (Organization) — drives Google Knowledge Graph entity
  resolution and disambiguates from Jordan Peterson. Keep schema intact.
- Voice: publication-grade scholarly register (see depth voice-guides).
  HARD RULE: "depth psychology scholar," never "depth psychologist," until
  Cody is clinically licensed. Never hallucinate citations — only verified
  Peterson publications.
- Deep history: `~/projects/depth/CLAUDE.md` + depth memory dir.
